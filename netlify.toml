[build]
  base = ""
  command = "npm install -g pnpm && pnpm install --no-frozen-lockfile && pnpm --filter @workspace/portfolio run build"
  publish = "artifacts/portfolio/dist/public"

[build.environment]
  NODE_VERSION = "20"
  BASE_PATH = "/"
